# 2026-09-03

## 英雄军鼓"攻防血生效2秒后回归"根因修复（限时属性缺续期 + 补独立定时器89）
- 用户复测：攻/防/血随军鼓生效，约2秒后**完全回归**未穿数值。关键现场：测试时**玩家本体不带军鼓，只英雄穿14位**。
- 根因(翎风可复用机制)：`H.ChangeHumAbility 属性 = 值 3` 第4参=**时间秒**，即 3 秒限时属性(340章)。引擎按自身周期重算英雄属性会把它冲掉，必须靠**周期续期重施**顶住。玩家侧军鼓靠穿戴时 `SetOnTimer 87 2` 续期；英雄军鼓穿戴(HeroTakeOnEx)只调刷新、**未启动任何专属定时器**，续期完全寄生于"玩家是否带军鼓/登录87是否在跑"。玩家不带军鼓 → 无续期 → 3秒限时在约2秒后被引擎重算覆盖 = 回归。
- 次要隐患：原 `@英雄军鼓_定时检查`/`_战斗确认` 在**玩家作用域**用 `H.CHECKUSEITEM 14`/`H.GetItemFieldValue` 探测英雄14位装备；全知识库+历史更新记录中**无这两个 H. 版命令**(历史有 H.CHECKLEVELEX/H.CHECKFENGHAO/H.CHECK 等，独无 H.CHECKUSEITEM/H.GetItemFieldValue)——在玩家作用域不可靠。
- 根治方案：给英雄军鼓**独立、自启动自关闭的专属续期定时器 89**(玩家军鼓占87,89空闲,已核实现用1-10/85-88)：
  - `@英雄军鼓_刷新` 成功应用末尾 `SetOnTimer 89 2`(穿戴/登录事件触发)。
  - `@英雄军鼓_清理` 收尾 `SetOffTimer 89`(卸下/失效关闭)。
  - QManage 新增 `[@OnTimer89]`→`#CALL @英雄军鼓_定时检查`(插在[@OnTimer88]前)。
  - 重构 `@英雄军鼓_定时检查`：删掉玩家作用域 H.CHECKUSEITEM/H.GetItemFieldValue 探测，改为仅 `N$英雄军鼓有效==1` 则重施应用常驻+刷攻速/施法速度，否则 `SetOffTimer 89` 自停。装备在位交给穿戴/卸下两个**英雄作用域事件**维护。
  - OnTimer87 里的英雄调用保留作双入口兜底(幂等重施,无冲突)。
- 文件：军鼓BUFF.txt / QManage.txt，GBK无BOM+CRLF 保持。工具 `tools/fix_hero_jungu_renew2.py`(按行号区间插入/整段替换,避开多段相同"应用常驻+刷攻速+刷法速"三连文本的子串歧义)。备份 `.workbuddy/backup-英雄军鼓-20260903/{军鼓BUFF,QManage}.txt.pre-renew.bak`。check_hero_jungu_stage1.py 回归 0 失败。
- 复用教训：写英雄军鼓/英雄侧临时属性时，ChangeHumAbility 第4参是秒数=限时；需周期续期；续期定时器应独立于玩家军鼓/87,避免玩家不带军鼓时英雄加成无续期而回归。

## 英雄军鼓阶段1 实机 bug 修复（H.LockUpdateAbil/H.UpdateAbil 非法 → load fail）
- 用户实机复测报错：`script error, load fail: 系统功能\军鼓BUFF.txt@英雄军鼓_应用常驻`(连续4条)。
- 根因（**翎风可复用语法经验**）：`@英雄军鼓_应用常驻`/`@英雄军鼓_关闭当前表现` 里误用了 `H.LockUpdateAbil`/`H.UpdateAbil`。全知识库/历史更新记录证实**引擎只有无前缀 `LockUpdateAbil`/`UpdateAbil`（人物/英雄共用，锁的是属性系统整体批量刷新），不存在 `H.` 前缀版本**；带 H. 前缀即命令不存在→整段 load fail。而属性/伤害/攻速/读装备这类**有英雄专用命令**才用 H. 前缀（如 `H.ChangeHumAbility`[340章实锤]、`H.ChangeDamageValue`、`H.CHANGESPEED`、`H.CHECKUSEITEM`、`H.GetItemFieldValue`）。
- 区分规则：**性能锁命令 LockUpdateAbil/UpdateAbil 永远无 H. 前缀**；改英雄属性等用 H.+具体命令。无法确认某命令有无 H. 版时，先搜全知识库历史更新记录与仓库现有成功脚本，别臆造。
- 次要：1595 行 `}[@英雄军鼓_应用常驻]` 合并段边界(全仓库唯一)拆成标准 `}` 换行 `[@...]`。
- 修复：`tools/fix_hero_jungu_loadfail.py`：H.LockUpdateAbil→LockUpdateAbil ×6、H.UpdateAbil→UpdateAbil ×6、拆分合并段 ×1。备份 `.workbuddy/backup-英雄军鼓-20260902/军鼓BUFF.txt.pre-loadfail.bak`。check_hero_jungu_stage1.py 回归 0 失败；全工程无 H.LockUpdateAbil/H.UpdateAbil 残留。GBK无BOM+CRLF 保持。
- 后续：需用户重新同步 `军鼓BUFF.txt` 到运行服务器(D:\MirServerLD12，本机无)再复测穿戴/登录常驻生效。

## 英雄军鼓阶段2：受击减伤战斗闭环已落地
- 用户选择"直接写阶段2受击减伤"。已落地（改 `军鼓BUFF.txt` + `QFunction-0.txt`，GBK+CRLF 安全写回）：
  - 重建 `@英雄军鼓_读取品质数值`：按英雄军鼓职业 warrior/wizard/taoist 分发玩家同款品质数值到独立 N$英雄军鼓* 域（含触发几率/铁头减伤/元素/恢复 等战斗字段，数值与玩家军鼓品质基础一致）。
  - 新增 `@英雄军鼓_战斗确认`(英雄在线+在位+有效+名称一致) 与 `@英雄军鼓_被打掉血前`。
  - QF 末尾新增 `[@HeroStruckDamage]`→`@英雄军鼓_被打掉血前`。受击减伤用 `H.ChangeDamageValue 1 - X`(手册796实锤)，回血 `H.HumanHP +`；RANDOMEX/LARGE/EQUAL 条件命令均放 #IF 区(吸取 GetStringPosEX 教训)。
- 工具：`tools/apply_hero_jungu_stage2.py`(落盘)、check 工具已扩展新增标签，静态回归 0 失败。备份 `.workbuddy/backup-英雄军鼓-20260902/*.pre-stage2.bak`。交付文档 docs/英雄军鼓落地阶段2改动说明.md。
- 尚未落地(阶段3/阶段2攻击向)：@HeroAttackDamage 攻击对怪切割(拆门板/剁馅机)、法师 RangeHarm 满层冰冻、道士毒嘴子/养娃人召唤联动、H.AddHumNewValue 隐藏常驻层、H.SETICON 图标。均依赖英雄攻击目标操作/技能判断等命令实机确认。
- 部署注意：用户实机运行副本在 D:\MirServerLD12(本机不存在)，改完本地仓库需用户重新同步部署军鼓BUFF.txt 与 QFunction-0.txt 再复测。

## 英雄军鼓阶段1 实机 bug 修复（GetStringPosEX 条件命令落区）
- 用户实机复测报错：`GetStringPosEX ..\QuestDiary\系统功能\军鼓流派配置.txt <$STR(S$英雄军鼓名称)> N$英雄军鼓配置行 S$英雄军鼓配置文本 0` 脚本错误，行号落在运行副本 D:\MirServerLD12 的 QFunction/QManage #CALL 展开处（9973/3605/4455/4551），本机无该目录（用户在别机部署运行）。
- 根因（**翎风可复用语法经验**）：`GetStringPosEX` 是**条件命令**，必须写在 `#IF` 区作判断，不能放 `#ACT` 动作区。玩家侧 `军鼓流派配置.txt` 的 `@军鼓配置_读取` 即 `#IF GetStringPosEX ...` 结构。同类要注意：能返回 true/false 的检测类命令（CHECKUSEITEM/GetStringPosEX/CheckHeroOnline/CheckHeroJob/EQUAL/RANDOMEX/CHECKCURRTARGETRACE）都放 #IF；无返回值的动作命令（GetItemFieldValue/ExtractStringEx/ChangeHumAbility/MOV/#CALL）放 #ACT。误把条件命令当动作用是引擎"脚本错误"常见根因。
- 修复：`军鼓BUFF.txt` 英雄军鼓_刷新 内配置解析段改为与玩家侧同构（#IF GetStringPosEX → 空#ACT → EQUAL 配置行=0 判断未命中→清理），全落英雄独立变量域。工具：`tools/fix_hero_jungu_getstringpos.py`。已备份 `.workbuddy/backup-英雄军鼓-20260902/军鼓BUFF.txt.pre-fix-getstringpos.bak`。check_hero_jungu_stage1.py 重跑仍 0 失败。
- 后续：需用户把修复后的 `军鼓BUFF.txt` 重新同步到运行服务器再复测。阶段2(战斗)/3(表现) 依赖阶段1 复测确认 H.CHECKUSEITEM/H.GetItemFieldValue 可用性后再落地，避免重复踩命令落区/不支持坑。
- 阶段2技术前提已核实：英雄攻击掉血前改伤 `H.ChangeDamageValue`(手册796)、英雄当前目标判断 `H.CHECKCURRTARGETRACE`(手册103) 均已实锤。英雄品质战斗数值桶需补全（切割/触发几率/减伤/物魔减等，与玩家同品质同值，落 N$英雄军鼓* 域）。法师/道士方向攻击依赖元素/技能/召唤等英雄命令，未逐条实锤，属高风险区。

## 英雄军鼓阶段2攻击向(@HeroAttackDamage)代码已落地(待实机)
- 用户问"没有问题，继续后面的阶段"，经 AskUserQuestion 选"战士切割+全职业加伤核心"。已落地改 `军鼓BUFF.txt` + `QFunction-0.txt`，GBK无BOM+CRLF 安全写回：
  - 英雄 wizard/taoist 品质桶补 `N$英雄军鼓百分`(攻击加伤,与玩家同品质同值: wizard 2/2/3/3/4/5, taoist 1/2/2/3/3/4; 含各品质分支+顶部清空0,共14处)。
  - 文末新增 `@英雄军鼓_攻击掉血前`: 战士拆门板/剁馅机 目标非玩家+几率 `H.M.HumanHP - 切割/追风切割`(对当前攻击怪直扣最大生命比例,手册164 H.M.多级运用实锤); 法师小冰箱/电耗子/火葫芦 + 道士毒嘴子/养娃人 按几率 `H.ChangeDamageValue 1 + 百分` 通用加伤(不细分技能,手册796 H.ChangeDamageValue 实锤)。法师/道士采用技能名无关通用触发(未用 CHECKMAGICNAME 英雄版细分)。
  - QF 末尾新增 `[@HeroAttackDamage]`→`#CALL @英雄军鼓_攻击掉血前`(在 @HeroStruckDamage 之后,对称)。
- 工具: `tools/apply_hero_jungu_attack.py`(落盘,含 wziard/taoist fill_pct 品质百分填充), `tools/check_hero_jungu_attack.py`(静态回归 0 失败)。check_hero_jungu_stage1.py 也纳入新#CALL,0失败。备份 `.workbuddy/backup-英雄军鼓-20260903/*.pre-attack.bak`。交付 docs/英雄军鼓落地阶段2攻击向改动说明.md。
- **踩坑复盘(可复用)**: 用 Python 编辑服务端脚本,必须让 lines 列表(元素不含换行)作为唯一状态,所有插入针对 lines,最后 '\r\n'.join 写回; 绝不能把含 '\n' 的多行字符串直接 += 到已 CRLF 的文本——会造成混合换行(攻击段被塞成单行含 \n), 且 fill_pct 先改 lines 未同步回 text 导致百分没写入。多处段编辑行号会因前次插入偏移, 第二次 fill 前须重新 sect_bounds。先 cp 备份恢复再重跑修正脚本。
- 阶段3+ 尚未落地: H.SETICON 顶戴图标(手册750已实锤可用,可阶段3做; 但 BUFF栏 H.SetArrBuff 未实锤,降级只做顶戴; 需实机确认军鼓美术 273-287 在英雄端加载)、法师满层 RangeHarm/火葫芦层数、道士毒印状态与毒爆、养娃人英雄宝宝(@HeroSlaveAttack)。均需阶段1+2 实机通过后据实补。
