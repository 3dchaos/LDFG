# 2026-09-02 工作日志

## 道士军鼓导致宝宝不动问题排查

- 现象：道士佩戴「养娃人」和「奶不死」军鼓后，原始召唤宝宝（骷髅/神兽/圣兽/月灵）不再动；战士/法师军鼓无此问题。
- 已排除：养娃人的 `RECALLMOB 雪域战将 1 60 0 0 0 1`（用户实测非根因）。
- 排查文件：
  - `Mir200/Envir/QuestDiary/系统功能/军鼓流派/道士军鼓.txt`
  - `Mir200/Envir/QuestDiary/系统功能/军鼓BUFF.txt`
  - `Mir200/Envir/Market_def/QFunction-0.txt`（`[@SlaveAttackDamage]` 触发 `@军鼓BUFF_宝宝掉血前`）
- 关键链路：戴鼓 → `SetOnTimer 87 2`（每2秒）→ `@军鼓BUFF_定时检查` → `@军鼓BUFF_定时续期` → `@军鼓taoist_定时续期`。
- 最可疑命令：`@军鼓taoist_定时续期` 第一段每2秒无条件 `LockUpdateAbil` + `ChangeHumAbility 9/10 = 道术` + `UpdateAbil` 强制刷新人物属性，触发道士宝宝继承道术的重算，打断宝宝行动。
- 次要可疑：`BB.HumanHP`（奶不死刷新）、`CHECKSLAVECOUNT`（养娃人召唤检查）。
- 【已坐实】用户最新实测：毒嘴子方向**也**导致宝宝卡死不动 → 彻底排除养娃人 RECALLMOB、奶不死 BB.HumanHP、毒嘴子专属属性等方向分支，根因锁定为三个方向共有、每2秒无条件执行的第129-142行属性刷新段。
- 【进一步排除】用户又排除了 ChangeHumAbility。三个方向共有且每2秒执行的命令仅剩：AddHumNewValue(11/13/16/18/19/20/24)、LockUpdateAbil/UpdateAbil。
- 关键命令穷尽：道士军鼓含 ChangeState(仅毒嘴子攻击M.ChangeState 5，非共有)、KillCallMob(养娃人关闭)、BB.HumanHP(奶不死)、HumanHP/HumanMP(奶不死/毒嘴子刷新)。
- 定时器生命周期：QManage [@OnTimer87] 每2秒 → MOV N$军鼓定时刷新中 1 → #CALL @军鼓BUFF_定时检查 → 军鼓BUFF_定时续期 → 道士军鼓_定时续期 → MOV N$军鼓定时刷新中 0。标志位防止 SetOnTimer 87 2 重入。
- 宝宝触发回调：[@SlaveAttackDamage]（宝宝攻击掉血前，上下文为人）→ @军鼓BUFF_宝宝掉血前 → @军鼓BUFF_战斗确认。CHECKJOB/GetItemFieldValue 在宝宝上下文读的是人物，不误作用宝宝。
- 最终确认关键命令：`@军鼓taoist_定时续期` 第 129-142 行，每2秒无条件执行 `LockUpdateAbil` + `ChangeHumAbility 9/10 = 道术(时间参数3秒)` + `UpdateAbil`；道士宝宝攻击力继承主人道术，高频属性重算打断宝宝 AI 跟随/行动。
- 宝宝触发回调：`[@SlaveAttackDamage]`（QFunction-0.txt 第168-171行）→ `@军鼓BUFF_宝宝掉血前`（军鼓BUFF.txt 第734行）→ `@军鼓BUFF_战斗确认`（第786行）→ 仅养娃人方向有 `@军鼓taoist_宝宝_养娃人` 分支处理。
- 引擎命令语义依据：`BB.`=宝宝多级命令前缀（仅执行命令不支持检测，多宝宝随机取一）；`ChangeHumAbility 9/10`=道术下限/上限；`UpdateAbil`=属性解锁刷新。
- 【进一步排除】用户又排除了 AddHumNewValue。至此三个方向共有、每2秒执行的"属性命令"（RECALLMOB/ChangeHumAbility/AddHumNewValue）全部排除。
- 关键新线索：用户实测"切换战士军鼓或卸下军鼓，宝宝立即恢复前进" → 说明是持续可逆机制，非一次性打死/夺走宝宝。
- 道士 vs 战士/法师定时续期差异：战士/法师在定时续期额外 #CALL `@军鼓BUFF_刷新战士攻速`/`@军鼓BUFF_刷新法师施法速度`（ChangeSpeed），道士仅 #CALL `@军鼓taoist_定时续期`。
- 剩余未排除嫌疑（静态分析收敛）：① `LockUpdateAbil`+`UpdateAbil` 配对命令本身；② 每2秒定时器 `SetOnTimer 87 2` 高频循环对宝宝AI的干扰；③ `[@SlaveAttackDamage]`（宝宝攻击掉血前）→ `@军鼓BUFF_宝宝掉血前` → `@军鼓BUFF_战斗确认` 的宝宝上下文反复触发。
- 道士军鼓三个方向共有、每2秒执行、且唯一未被排除的命令对：`LockUpdateAbil`（第129行）+ `UpdateAbil`（第164行）。

## 安装 Mir200 知识 Skill
- 从 GitHub 安装了 `3dchaos/LfMirSkill`（Codex 格式 skill，内层 `lf-mir200-knowledge`）。
- 安全审计结论：P2（安全）。仅含文档 + 纯静态 Python 工具 `scripts/lf_kb.py`（索引/搜索/校验，无网络、无 shell、无 eval、无凭据访问）。安装脚本 `install_codex_skill.py` 仅做文件拷贝。
- 安装位置（用户级）：`C:\Users\Administrator\.workbuddy\skills\lf-mir200-knowledge\`
- 已添加 `agent_created: true` 以便后续通过 SkillManage 维护。
- 已运行 `lf_kb.py update` + `validate`：849 个说明书章节索引、8 节训练课，`validate` 返回 ok=true。
- 用法：在 Mir200/LF 脚本相关问题触发时，该 skill 会加载本地说明书知识库（knowledge_base/chapters）与 references（mir200-thinking、script-deadloop-debugging 等）。可选本地样本目录 `样本Mir200/` 未随公开包发布。
- 网络注意：git 默认 socks5 代理(7897)已失效；克隆需 `git -c http.proxy= -c https.proxy= clone`（走 HTTPS_PROXY 52871 才能通）。

## 英雄军鼓效果落地计划书
- 用户反馈"英雄佩戴军鼓不生效"。核实：军鼓 StdMode65、14号位；玩家侧链路完整（QFunction-0 的 TakeOnEx/TakeOffEx/AttackDamage/StruckDamage/PlayDie + QManage 的 OnTimer87/Login + 军鼓BUFF.txt + 军鼓流派配置 + 三方向军鼓脚本），英雄侧完全无接入（无 @HeroTakeOnEx/@HeroAttackDamage 等回调，仅 @HeroLogin 给战士英雄固定攻速3）。
- 设计取向（用户确认）：英雄独立穿戴自己的军鼓（复用54件物品）、复用玩家方向数值、现有固定攻速补偿并入新英雄军鼓链路。
- 英雄可用回调（手册）：@HeroTakeOnEx/@HeroTakeOffEx(742)、@HeroAttackDamage/@HeroStruckDamage+H.ChangeDamageValue(796)、@HeroLogin(709)、@HeroDie(711)、CheckHeroJob(486)、H.SETICON(750)、H.ChangeHumAbility(340)。方向触发重构后不依赖技能名（目标非玩家+概率+切割），对英雄复刻友好。
- 交付：docs/英雄军鼓效果落地计划书.md（阶段0前置可行性→阶段4工具回归）。

## 英雄军鼓落地·阶段1（最小常驻闭环）已实现
- 目标：英雄14号位独立穿军鼓后常驻属性生效（复用玩家方向数值、独立英雄变量域"英雄军鼓"隔离、攻速固定3收敛到统一汇总）。
- 改动文件（均GBK无BOM+CRLF，用Python gbk安全写回，勿用UTF-8覆写）：
  - `Mir200/Envir/QuestDiary/系统功能/军鼓BUFF.txt`：删除玩家固定攻速补偿`@军鼓BUFF_刷新战士英雄攻速`实现，改为`@英雄军鼓_刷新战士/法师基础..攻速/施法速度`统一汇总+旧标签转发；末尾追加`@英雄军鼓_刷新/职业确认/读取品质数值/应用常驻/定时检查/清理/死亡检查/关闭当前表现`整套。品质1-6数值内联(与玩家同值)。
  - `Mir200/Envir/Market_def/QFunction-0.txt`：末尾追加`[@HeroTakeOnEx](位置<$H.CurItemPos>=14)`、`[@HeroTakeOffEx]`、`[@HeroDie]` 英雄回调。
  - `Mir200/Envir/MapQuest_def/QManage.txt`：`@HeroLogin` 固定攻速块改调`@英雄军鼓_刷新`；`@OnTimer87` 追加`@英雄军鼓_定时检查`在线续期(复用玩家87定时器，不占新号)。
- 关键设计：英雄军鼓刷新**不复用玩家@军鼓配置_读取**(避免污染玩家S$军鼓*/N$军鼓*)，改用 GetStringPosEX+ExtractStringEx 独立解析，全落"英雄军鼓"变量域。
- 需实机验证命令（已注释标注）：H.CHECKUSEITEM / H.GetItemFieldValue 英雄前缀；方向隐藏层 H.AddHumNewValue、战斗触发 H.ChangeState/H.CHECKCURRTARGETRACE、法师/道士英雄技能名判断属阶段2未做。
- 工具（tools/）：`gbkpatch.py`通用GBK安全补丁；`apply_hero_jungu_stage1.py`+`..._qf_qm.py`+`fix*`一次性落盘脚本；`check_hero_jungu_stage1.py`静态回归(0失败)。
- 备份：`.workbuddy/backup-英雄军鼓-20260902/`。
- 阶段2(战斗方向镜像)/3(图标表现) 因依赖未实测英雄命令，待用户实机验证阶段1后继续；阶段4工具回归已由 check 脚本初步覆盖。
